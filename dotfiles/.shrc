alias ll="ls -lh"
alias la="ls -lAh"
alias l="ls -lh"

alias greppy="find . -name '*.py' | xargs grep --color"
xgrep() {
    find . -name "*.$1" | xargs grep ${@:2}
}
alias grp=xgrep

alias pv="open -a Preview"

# https://stackoverflow.com/a/26482258/1232944
alias ipy="python -c 'import IPython; IPython.terminal.ipapp.launch_new_instance()'"

# added by Miniconda2 4.1.11 installer
export PATH="/Users/joshchen/.local/share/miniconda2/bin:$PATH"

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/joshchen/lib/google-cloud-sdk/path.zsh.inc' ]; then source '/Users/joshchen/lib/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/joshchen/lib/google-cloud-sdk/completion.zsh.inc' ]; then source '/Users/joshchen/lib/google-cloud-sdk/completion.zsh.inc'; fi

# Work specific stuff
alias cd-merc="cd $HOME/proj/merantix/core"
source $HOME/proj/merantix/core/developer_env/setup.sh
